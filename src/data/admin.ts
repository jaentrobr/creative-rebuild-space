// Dados falsos removidos. Esta área do painel admin usa exclusivamente
// consultas reais ao Supabase (ver src/lib/admin-store.ts e as rotas em
// src/routes/admin.*.tsx). Este arquivo permanece só para não quebrar
// imports antigos e não deve mais conter dados mockados.
export {};
